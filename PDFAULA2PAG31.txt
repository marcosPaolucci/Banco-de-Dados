CREATE TABLE FUNCIONARIOS
( MATRICULA NUMBER(5) CONSTRAINT EMP_MATRICULA_PK1 PRIMARY KEY,
  COD_DPTO  NUMBER(4) CONSTRAINT EMP_COD_DEPT_NN1 NOT NULL,
  COD_CARGO NUMBER(4) CONSTRAINT EMP_COD_CARGO_NN1 NOT NULL,
  GERENTE   NUMBER(5) ,
  NOME VARCHAR2(50) NOT NULL,
  CPF NUMBER(11) NOT NULL 
  CONSTRAINT FUNCIONARIOS_CPF_CK CHECK (CPF > 0),
  RG CHAR(15) NOT NULL
  );
 
ALTER TABLE FUNCIONARIOS
 ADD CONSTRAINT FUNCIONARIOS_COD_DPTO_FK1
 FOREIGN KEY(COD_DPTO)
 REFERENCES DEPARTAMENTOS; 
  
ALTER TABLE FUNCIONARIOS
 ADD CONSTRAINT FUNCIONARIOS_COD_CARGO_FK1
 FOREIGN KEY(COD_CARGO)
 REFERENCES DEPARTAMENTOS; 
  
CREATE TABLE CARGOS
(COD_CARGO NUMBER(4) CONSTRAINT CARGO_PK PRIMARY KEY,
 DESC_CARO VARCHAR2(30) NOT NULL
 );
 

CREATE TABLE DEPARTAMENTOS
(COD_DPTO NUMBER(4) CONSTRAINT COD_DPTO PRIMARY KEY,
DESC_DPTO VARCHAR2(30) NOT NULL
);
DROP TABLE CARGOS;
DROP TABLE DEPARTAMENTOS;
DROP TABLE FUNCIONARIOS;

DESC CARGOS;
INSERT INTO CARGOS
VALUES (1, 'TI');
INSERT INTO CARGOS
VALUES (1, 'TESTE');

DESC FUNCIONARIOS;

